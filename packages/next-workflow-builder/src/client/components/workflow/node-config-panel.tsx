'use client';

import React from 'react';

export interface NodeConfigPanelProps {
  className?: string;
}

export function NodeConfigPanel({ className }: NodeConfigPanelProps) {
  // TODO: Port node configuration panel from Vercel template
  return (
    <div className={className} data-node-config-panel>
      <p>Node Config Panel — coming soon</p>
    </div>
  );
}
