// TODO: Replace with a real Better Auth instance
// Example: import { betterAuth } from 'better-auth';
export const auth = {
  api: {
    getSession: async (_opts: { headers: Headers }) => null,
  },
};
